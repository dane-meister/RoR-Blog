<!DOCTYPE html>
<html>
<head>
    <title><%= @article.title %></title>
</head>
<body>
    <main>
        <h1><%= @article.title %></h1>
        <p><%= @article.body %></p>

        <p>
            <%= link_to "Edit", edit_article_path(@article), class: 'edit-link' %> |
            <%= link_to "Destroy", article_path(@article), data: {
                            turbo_method: :delete,
                            turbo_confirm: "Are you sure?"
                        }, class: 'destroy-link' %>
        </p>

        <h2>Comments</h2>
        <% if @article.comments.size.zero? %>
            <p>No comments yet...</p>
        <% else %>
            <%= render @article.comments %>
        <% end %>

        <h2>Add a comment:</h2>
        <%= render "comments/form" %>
        <br>
        <%= link_to 'Back', articles_path, class: 'back-link' %>
    </main>
</body>
</html>
